import styled from 'styled-components';

export const StatsView = styled.main`
  width: 100%;
  max-width: 900px;
  margin: 2rem auto;

  section {
    margin: 2rem 0rem;

    h3 {
      color: var(--cian);
    }
  }
`;
